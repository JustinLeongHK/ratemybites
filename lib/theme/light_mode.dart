import 'package:flutter/material.dart';

ThemeData lightmode = ThemeData(
  colorScheme: ColorScheme.fromSeed(
      seedColor: const Color.fromARGB(197, 38, 146, 228),
      brightness: Brightness.light),
  useMaterial3: true,
);
